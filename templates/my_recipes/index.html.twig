{% extends 'base.html.twig' %}

{% block body %}
	<h2>This is my recipes page</h2>

	<div>
		{% for recipe in recipes %}
			<h4>
				{{ recipe.id }}.
				{{ recipe.name }}
			</h4>
		{% endfor %}
	</div>
{% endblock %}
